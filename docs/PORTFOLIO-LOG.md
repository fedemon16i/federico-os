# Portfolio log — federico-os

Append-only. Newest first. Chat B unless noted.

## 2026-08-29

- Knowledge sources: `fedemon16i/federico-skills` (Accessibility-Standards, knowledge/) and `fedemon16i/ai-capability-os` (ui-integrity-guardian, token-efficiency). Use these before inventing rules.
- DS page `ds.html`: English. Project → Light/Dark → Web/Tablet/Mobile → Library | Diagrams.
- Diagrams tab is boards only (Measure, Research, Understand, Funnel). Analyze / Hub / Pub stay out.
- Light diagrams: canvas cool gray `#e8edf2`. `--ey-yellow` in light = `#6B5200`. Bright yellow only as fill + `#111` ink.
- Research replay rebuilt: Pendo-like player contains a uniformly scaled `ey-02-analyze.html` (960×600 → frame). Interviews + CSAT/NPS stay. Task-study chip row dropped to give the tape height.
- Orders still open: `docs/DS-ORDERS.md` — finish kits in `fm-ds-sheet.html` for EY rail/forms and DollarCity POS.
- Do not touch Chat A (home OS, nav, monitor).

## Open

1. Complete component library per project (EY then DollarCity).
2. Confirm Analyze cursors still move inside the scaled tape (iframe + EY.fit).
3. Customs still outside original once/window contract if that work resumes.
