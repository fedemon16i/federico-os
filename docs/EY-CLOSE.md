# EY close checklist

## Law
Quiet plate → ink. Photo plate → white type.
No light-on-light. No dark-on-dark. Type, icons, borders, lines, charts.
Drop-off is always BAD red, both modes.

## Two systems
- Product: `ey-ds.css` / `fm-ds.css` — `ds.html`
- Showcase: `fm-show.css` — `show.html` (cursor, veil, callout, brands)

## Beats
Live inside Projects DS, last section, per project.
`beats.html` redirects to `ds.html#beats`.

## Contrast
- [x] Chip / button hover = invert ink/plate
- [x] Tag bad / veil fail = `#8b1e1e` + `#fde8e8`
- [x] Hero / tiles = white type
- [x] Light cursor label = BAD red, not yellow
- [x] Veil pops (scale + fade)
- [ ] Analyze local hex (`#fff` rail) still in file; DS `!important` covers light if cache is fresh
- [ ] Beat HTML still uses `.btn-w` not `.fm-btn` — aliased, not renamed

## Missing from library vs beats (diagrams, keep separate)
Sitemap node, funnel row, research replay chrome, design canvas, understand findings.

## Next project
DollarCity product DS, same invert hover + contrast law.
